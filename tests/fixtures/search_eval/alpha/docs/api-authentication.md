# API Authentication

Clients send bearer tokens to protected endpoints.
The integration starts with the login handshake in [[alpha/docs/authentication-overview]].
Use the Authorization header for every authenticated request.
