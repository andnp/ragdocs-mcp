# Authentication Overview

Authentication verifies identity before protected actions are allowed.

## Flow

The login flow issues an access token and a refresh token.
See [[alpha/docs/token-lifecycle]] for expiration rules.
See [[alpha/docs/api-authentication]] for API integration details.
